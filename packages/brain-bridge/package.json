{
  "name": "@preston/brain-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.ts",
    "test": "node --test test/bridge.test.ts",
    "check": "node --check src/config.ts && node --check src/types.ts && node --check src/client.ts && node --check src/protocol.ts && node --check src/server.ts"
  }
}
