{
  "name": "hermes-preston-supervisor",
  "version": "0.1.0",
  "private": true,
  "description": "Preston Supervisor - read-only Preston Control module for the official Hermes Agent dashboard (drop-in plugin)",
  "scripts": {
    "build": "node build.mjs",
    "test": "node build.mjs && vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.9"
  }
}
